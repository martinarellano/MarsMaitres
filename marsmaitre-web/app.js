let restaurants=[
 {name:'La Terraza Norte',city:'Ciudad de México',plan:'Profesional',status:'Activo',users:6,minutes:'428 / 600'},
 {name:'Sabor de Barrio',city:'Guadalajara',plan:'Inicio',status:'Prueba',users:2,minutes:'61 / 150'},
 {name:'Casa del Fuego',city:'Monterrey · 3 sucursales',plan:'Cadenas',status:'Activo',users:14,minutes:'1,204 / 2,000'}
];
let calls=[['La Terraza Norte','Pedido · 8 min','Pedido confirmado','98%'],['Casa del Fuego','Reservación · 4 min','Creada en Calendar','96%'],['Sabor de Barrio','Pregunta de menú · 2 min','Transferida a empleado','91%']];
let selectedRestaurantId=localStorage.getItem('mars_selected_restaurant')||'';
const app=document.querySelector('#app'), title=document.querySelector('#page-title'), toastBox=document.querySelector('#toast');
function myUrl(path){const id=selectedRestaurantId;if(!id)return path;return path+(path.includes('?')?'&':'?')+'restaurant_id='+encodeURIComponent(id)}
function money(n){return '$'+n.toLocaleString('es-MX')}
function notify(text){toastBox.textContent=text;toastBox.classList.add('show');setTimeout(()=>toastBox.classList.remove('show'),2400)}
function nav(view){document.querySelectorAll('.nav').forEach(x=>x.classList.toggle('active',x.dataset.view===view));render(view)}
document.querySelectorAll('.nav').forEach(x=>x.addEventListener('click',()=>nav(x.dataset.view)));
function head(titleText,action=''){return `<div class="section-head"><h2>${titleText}</h2>${action?`<button class="button small" data-action="${action}">${action==='new'?'＋ Registrar restaurante':action==='plans'?'Administrar planes':''}</button>`:''}</div>`}
function overview(){return `<div class="grid metrics"><div class="metric"><small>Restaurantes activos</small><strong>3</strong><em>↑ 1 este mes</em></div><div class="metric"><small>Suscripciones</small><strong>3</strong><em>100% al corriente</em></div><div class="metric"><small>Llamadas este mes</small><strong>1,693</strong><em>↑ 18.4%</em></div><div class="metric"><small>Ingresos mensuales</small><strong>$4,697</strong><em>MXN · recurrente</em></div></div><div class="section-head"><h2>Estado de la plataforma</h2></div><div class="status-card"><div><strong>Todos los servicios operativos</strong><p>Google Calendar conectado · grabaciones protegidas · respaldo activo</p></div><span class="status">ACTIVO</span></div>${head('Restaurantes recientes','new')}<div class="table-wrap"><table class="table"><thead><tr><th>Restaurante</th><th>Plan</th><th>Uso de minutos</th><th>Estado</th></tr></thead><tbody>${restaurants.map(r=>row(r)).join('')}</tbody></table></div><div class="grid two">${head('Llamadas recientes')||''}<div class="card">${calls.map(c=>call(c)).join('')}</div><div class="card"><h3>Uso de minutos</h3><p class="label">Consumo global · 1,693 de 2,750</p><div class="bar"><i style="width:62%"></i></div><p class="label" style="margin-top:20px">Meta de margen operativo</p><div class="bar"><i style="width:68%"></i></div><p style="font-size:12px;color:var(--muted)">Revisar costos de telefonía y voz antes de superar el 75%.</p></div></div>`}
function row(r){return `<tr><td><div class="restaurant"><span class="restaurant-icon">${r.name[0]}</span><div><strong>${r.name}</strong><small style="display:block;color:var(--muted);margin-top:3px">${r.city}</small></div></div></td><td><span class="pill ${r.plan==='Inicio'?'amber':r.plan==='Cadenas'?'blue':'green'}">${r.plan}</span></td><td>${r.minutes}</td><td><span class="pill ${r.status==='Prueba'?'amber':'green'}">${r.status}</span></td></tr>`}
function call(c){return `<div class="call"><span class="call-icon">☎</span><div><strong>${c[0]}</strong><small>${c[1]} · ${c[2]}</small></div><span class="score">${c[3]}</span></div>`}
function restaurantsView(){return `${head('Todos los restaurantes','new')}<div class="table-wrap"><table class="table"><thead><tr><th>Restaurante</th><th>Plan</th><th>Usuarios</th><th>Minutos</th><th>Acciones</th></tr></thead><tbody>${restaurants.map(r=>`<tr><td><div class="restaurant"><span class="restaurant-icon">${r.name[0]}</span><strong>${r.name}</strong></div></td><td>${r.plan}</td><td>${r.users}</td><td>${r.minutes}</td><td><button class="button secondary small" data-action="detail">Ver ficha</button>${r.status==='Prueba'?` <button class="button small" data-action="activate-cash" data-id="${r.id}">Activar efectivo</button>`:''} <button class="button secondary small" data-action="invite-owner" data-id="${r.id}">Crear acceso</button></td></tr>`).join('')}</tbody></table></div>`}
function subscriptions(){const plans=[['Inicio','$499','150 minutos · 2 usuarios',['Menú y preguntas frecuentes','Reservaciones','Historial 30 días']],['Profesional','$1,199','600 minutos · 8 usuarios',['Pedidos y venta sugerida','Reportes y retroalimentación','Historial 90 días']],['Cadenas','$2,999','2,000 minutos · 5 sucursales',['Panel central','Roles avanzados','Lista de espera']],['Empresarial','Desde $5,999','A la medida',['SLA y soporte prioritario','Integraciones','Políticas especiales']]];return `${head('Planes y suscripciones','plans')}<div class="grid plan-grid">${plans.map((p,i)=>`<article class="plan ${i===1?'highlight':''}">${i===1?'<span class="tag">MÁS ELEGIDO</span>':''}<h3>${p[0]}</h3><div class="price">${p[1]}<small style="font-size:11px;color:var(--muted);font-weight:500"> / mes</small></div><p>${p[2]}</p><ul>${p[3].map(x=>`<li>${x}</li>`).join('')}</ul><button class="button secondary small" data-action="select">Seleccionar plan</button></article>`).join('')}</div><div class="section-head"><h2>Reglas de facturación</h2></div><div class="card"><p style="margin-top:0;font-size:13px">Pago en efectivo con activación manual · prueba de 7 días · control de vencimiento · Mercado Pago queda pendiente hasta que el administrador lo habilite.</p><button class="button" data-action="billing">Configurar facturación</button></div>`}
function callsView(){return `${head('Llamadas AI')}<div class="card">${calls.concat(calls).map(c=>call(c)).join('')}</div>`}
function reports(){return `${head('Reportes globales')}<div class="grid metrics"><div class="metric"><small>Pedidos confirmados</small><strong>742</strong><em>43.8% de llamadas</em></div><div class="metric"><small>Reservaciones</small><strong>318</strong><em>Calendar sincronizado</em></div><div class="metric"><small>Satisfacción</small><strong>4.7/5</strong><em>Últimos 30 días</em></div><div class="metric"><small>Venta sugerida</small><strong>29%</strong><em>Aceptación de extras</em></div></div><div class="grid two"><div class="card"><h3>Actividad por restaurante</h3>${restaurants.map(r=>`<p class="label" style="margin-top:16px">${r.name}</p><div class="bar"><i style="width:${r.plan==='Cadenas'?82:r.plan==='Profesional'?67:29}%"></i></div>`).join('')}</div><div class="card"><h3>Alertas para administrador</h3><p>• 1 restaurante cerca del límite de minutos.</p><p>• 2 menús pendientes de actualización.</p><p>• 3 llamadas transferidas por queja.</p><button class="button secondary small" data-action="export">Exportar reporte</button></div></div>`}
function ownerDashboard(data){const r=data.restaurants.find(x=>x.id===selectedRestaurantId)||data.restaurants[0];selectedRestaurantId=r.id;localStorage.setItem('mars_selected_restaurant',selectedRestaurantId);const limit=data.limits[r.id]||{minutes:r.minutes_limit,users:2};const used=r.minutes_used||0;const pct=Math.min(100,Math.round(used/(limit.minutes||r.minutes_limit||1)*100));const myCalls=(data.calls||[]).filter(c=>c.restaurant_id===r.id);const selector=data.restaurants.length>1?`<label class="restaurant-switch">Restaurante / sucursal<select id="restaurant-switcher">${data.restaurants.map(x=>`<option value="${x.id}" ${x.id===r.id?'selected':''}>${x.name}</option>`).join('')}</select></label>`:'';return `<div class="section-head"><div><h2>Hola, ${data.user.name}</h2><p style="color:var(--muted);margin-top:4px">Panel privado de ${r.name}</p></div>${selector}</div><div class="grid metrics"><div class="metric"><small>Estado de cuenta</small><strong>${r.status}</strong><em>${r.plan}</em></div><div class="metric"><small>Minutos usados</small><strong>${used}</strong><em>de ${limit.minutes||r.minutes_limit}</em></div><div class="metric"><small>Usuarios</small><strong>${r.users_count}</strong><em>máximo ${limit.users}</em></div><div class="metric"><small>Sucursales</small><strong>1</strong><em>máximo ${limit.branches||1}</em></div></div><div class="grid two"><div class="card"><h3>Consumo del agente</h3><p class="label">${used} de ${limit.minutes||r.minutes_limit} minutos</p><div class="bar"><i style="width:${pct}%"></i></div><p class="label" style="margin-top:18px">${pct}% utilizado este periodo</p></div><div class="card"><h3>Acciones rápidas</h3><p>Configura el menú, horarios, reservaciones y respuestas del agente.</p><button class="button secondary small" data-action="owner-menu">Editar menú</button> <button class="button secondary small" data-action="owner-reservations">Reservaciones</button> <button class="button secondary small" data-action="owner-feedback">Retroalimentación</button> <button class="button secondary small" data-action="owner-calendar">Conectar Calendar</button></div></div><div class="section-head"><h2>Últimas llamadas</h2></div><div class="card">${myCalls.length?myCalls.map(c=>`<div class="call"><span class="call-icon">☎</span><div><strong>${c.result}</strong><small>${c.duration_min} min · ${new Date(c.created_at).toLocaleString('es-MX')}</small></div><span class="score">${c.confidence}</span></div>`).join(''):'<p>Aún no hay llamadas registradas.</p>'}</div>`}
async function ownerReservations(){const token=localStorage.getItem('mars_token')||'';const response=await fetch(myUrl('/api/my/reservations'),{headers:{'Authorization':'Bearer '+token}});const calendarResponse=await fetch(myUrl('/api/my/calendar'),{headers:{'Authorization':'Bearer '+token}});const calendarData=calendarResponse.ok?await calendarResponse.json():{connection:{}};const calendar=calendarData.connection||{};if(!response.ok){notify('No se pudieron cargar las reservaciones');return}const data=await response.json();app.innerHTML=`<div class="section-head"><div><h2>Reservaciones</h2><p style="color:var(--muted);margin-top:4px">Se guardan aquí y quedarán listas para sincronizar con Calendar</p></div><button class="button secondary small" data-action="owner-back">Volver al inicio</button></div><div class="grid two"><form class="card" id="reservation-form"><h3>Nueva reservación</h3><label>Nombre<input name="customer_name" required></label><label>Teléfono<input name="customer_phone"></label><label>Fecha y hora<input name="start_at" type="datetime-local" required></label><label>Personas<input name="party_size" type="number" min="1" value="2" required></label><label>Notas<textarea name="notes" rows="2"></textarea></label><button class="button" type="submit">Guardar reservación</button></form><div class="card"><h3>Google Calendar</h3><p>Estado: <strong>${calendar.status||'not_connected'}</strong></p><p style="color:var(--muted);font-size:13px">Cada restaurante deberá autorizar su propio calendario antes de crear eventos reales.</p><form id="calendar-form"><label>Identificador del calendario<input name="calendar_id" value="${calendar.calendar_id||''}" placeholder="primary o ID del calendario" required></label><button class="button secondary small" type="submit">Guardar y preparar conexión</button></form></div></div><div class="section-head"><h2>Próximas reservaciones</h2></div><div class="card">${data.items.length?data.items.map(x=>`<div class="call"><div><strong>${x.customer_name}</strong><small>${new Date(x.start_at).toLocaleString('es-MX')} · ${x.party_size} personas · ${x.customer_phone||'sin teléfono'}</small></div><span class="pill amber">${x.status}</span><div><button class="button secondary small" data-action="confirm-reservation" data-id="${x.id}">Confirmar</button> <button class="button secondary small" data-action="cancel-reservation" data-id="${x.id}">Cancelar</button></div></div>`).join(''):'<p>No hay reservaciones registradas.</p>'}</div>`;app.querySelector('#reservation-form').addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch(myUrl('/api/my/reservations'),{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify(body)});if(r.ok)ownerReservations();else notify('No se pudo guardar la reservación')});app.querySelector('[data-action="owner-back"]').addEventListener('click',connectPanel);app.querySelector('#calendar-form').addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch(myUrl('/api/my/calendar/prepare'),{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify(body)});if(!r.ok){notify('No se pudo preparar Calendar');return}const oauth=await fetch(myUrl('/api/my/calendar/oauth/start'),{headers:{'Authorization':'Bearer '+token}});const info=await oauth.json();if(oauth.ok&&info.authorization_url)window.location.href=info.authorization_url;else notify('Calendario guardado; falta configurar OAuth en el servidor público')});app.querySelectorAll('[data-action="confirm-reservation"],[data-action="cancel-reservation"]').forEach(b=>b.addEventListener('click',async()=>{const endpoint=b.dataset.action==='confirm-reservation'?'/api/my/reservations/confirm':'/api/my/reservations/cancel';const r=await fetch(myUrl(endpoint),{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify({id:b.dataset.id})});if(r.ok)ownerReservations();else notify('No se pudo actualizar la reservación')}))}
async function ownerFeedback(){const token=localStorage.getItem('mars_token')||'';const response=await fetch(myUrl('/api/my/feedback'),{headers:{'Authorization':'Bearer '+token}});if(!response.ok){notify('No se pudo cargar la retroalimentación');return}const data=await response.json();app.innerHTML=`<div class="section-head"><div><h2>Retroalimentación</h2><p style="color:var(--muted);margin-top:4px">Transcripciones y mejoras detectadas en las llamadas</p></div><button class="button secondary small" data-action="owner-back">Volver al inicio</button></div><div class="card">${data.items.length?data.items.map(x=>`<div class="call"><div><strong>${x.result||'Llamada procesada'}</strong><small>${x.duration_min||0} min · ${x.summary||'Sin resumen todavía'}${x.score!==null&&x.score!==undefined?' · Calificación '+x.score+'/5':''}</small>${x.improvements?`<small>Mejoras: ${x.improvements}</small>`:''}</div><span class="pill ${x.consented?'green':'amber'}">${x.consented?'Consentida':'Sin confirmación'}</span></div>`).join(''):'<p>Aún no hay llamadas ni transcripciones recibidas.</p>'}</div>`;app.querySelector('[data-action="owner-back"]').addEventListener('click',connectPanel)}
async function ownerEditor(){const token=localStorage.getItem('mars_token')||'';const response=await fetch(myUrl('/api/my/restaurant'),{headers:{'Authorization':'Bearer '+token}});if(!response.ok){notify('No se pudo cargar la configuración');return}const data=await response.json();const s=data.settings||{};app.innerHTML=`<div class="section-head"><div><h2>Configuración de ${data.restaurant.name}</h2><p style="color:var(--muted);margin-top:4px">Solo visible para tu restaurante</p></div><button class="button secondary small" data-action="owner-back">Volver al inicio</button></div><div class="grid two"><form class="card" id="settings-form"><h3>Datos del agente</h3><label>Saludo inicial<input name="greeting" value="${s.greeting||''}" placeholder="Hola, gracias por llamar..."></label><label>Teléfono del restaurante<input name="phone" value="${s.phone||'3521388304'}" placeholder="3521388304"></label><label>Operador móvil<input name="phone_carrier" value="${s.phone_carrier||'Telcel'}" placeholder="Telcel"></label><label>Proveedor de voz AI<input name="voice_provider" value="${s.voice_provider||''}" placeholder="Pendiente de elegir"></label><label>Número al que se desviarán las llamadas<input name="forwarding_number" value="${s.forwarding_number||''}" placeholder="Lo proporciona el proveedor VoIP"></label><label>Dirección<input name="address" value="${s.address||''}"></label><label>Horario<input name="opening_hours" value="${s.opening_hours||''}" placeholder="Lun-Dom 12:00-22:00"></label><label>Política de alérgenos<textarea name="allergen_policy" rows="3">${s.allergen_policy||''}</textarea></label><button class="button" type="submit">Guardar configuración</button></form><p style="color:var(--muted);font-size:13px">Telefonía: <strong>${s.voice_status||'not_configured'}</strong>. La línea Telcel se podrá desviar al número VoIP del proveedor cuando se elija.</p></div><div class="card"><h3>Agregar platillo</h3><form id="menu-form"><label>Nombre<input name="name" required></label><label>Categoría<input name="category" placeholder="Entradas, fuertes..."></label><label>Precio MXN<input name="price_mxn" type="number" min="0" step="0.01" required></label><label>Descripción<input name="description"></label><label>Ingredientes<input name="ingredients"></label><label>Alérgenos<input name="allergens"></label><button class="button" type="submit">Agregar al menú</button></form></div></div><div class="card" style="margin-top:18px"><h3>Probar respuestas del agente</h3><p style="color:var(--muted);font-size:13px">Simulación local con tu configuración actual; todavía no es una llamada real.</p><form id="agent-test"><label>Pregunta del cliente<input name="question" placeholder="¿Cuánto cuesta la hamburguesa?" required></label><button class="button secondary small" type="submit">Probar respuesta</button></form><p id="agent-answer" style="margin-bottom:0"></p></div><div class="section-head"><h2>Menú actual</h2></div><div class="card">${data.items.length?data.items.map(i=>`<div class="call"><div><strong>${i.name}</strong><small>${i.category||'Sin categoría'} · $${Number(i.price_mxn).toLocaleString('es-MX',{minimumFractionDigits:2})} · ${i.ingredients||'Ingredientes pendientes'}</small></div><button class="button secondary small" data-action="delete-item" data-id="${i.id}">Eliminar</button></div>`).join(''):'<p>Aún no hay platillos. Agrega el primero arriba.</p>'}</div>`;app.querySelector('#settings-form').addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch(myUrl('/api/my/settings'),{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify(body)});notify(r.ok?'Configuración guardada':'No se pudo guardar')});app.querySelector('#menu-form').addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch(myUrl('/api/my/menu'),{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify(body)});if(r.ok)ownerEditor();else notify('No se pudo agregar el platillo')});app.querySelector('#agent-test').addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch(myUrl('/api/my/agent/test'),{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify(body)});const result=await r.json();app.querySelector('#agent-answer').textContent=result.answer||result.error||'Sin respuesta'});app.querySelectorAll('[data-action="delete-item"]').forEach(b=>b.addEventListener('click',async()=>{await fetch(myUrl('/api/my/menu/delete'),{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify({id:b.dataset.id})});ownerEditor()}));app.querySelector('[data-action="owner-back"]').addEventListener('click',connectPanel)}
function accountView(){title.textContent='Mi cuenta';app.innerHTML=`<div class="section-head"><div><h2>Seguridad de la cuenta</h2><p style="color:var(--muted);margin-top:4px">Actualiza tu contraseña antes de entregar accesos a clientes.</p></div><button class="button secondary small" data-action="account-back">Volver</button></div><div class="card" style="max-width:560px"><form id="password-form"><label>Contraseña actual<input name="current_password" type="password" autocomplete="current-password" required></label><label>Nueva contraseña<input name="new_password" type="password" minlength="12" autocomplete="new-password" required></label><label>Repite la nueva contraseña<input name="confirm_password" type="password" minlength="12" autocomplete="new-password" required></label><button class="button" type="submit">Cambiar contraseña</button></form><hr style="border:0;border-top:1px solid var(--line);margin:24px 0"><button class="button secondary small" id="logout-button">Cerrar sesión</button></div>`;app.querySelector('#password-form').addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));if(body.new_password!==body.confirm_password){notify('Las contraseñas nuevas no coinciden');return}const response=await fetch('/api/account/change-password',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+(localStorage.getItem('mars_token')||'')},body:JSON.stringify({current_password:body.current_password,new_password:body.new_password})});const data=await response.json();notify(response.ok?'Contraseña actualizada':(data.error||'No se pudo cambiar la contraseña'));if(response.ok)e.target.reset()});app.querySelector('#logout-button').addEventListener('click',async()=>{await fetch('/api/logout',{method:'POST',headers:{'Authorization':'Bearer '+(localStorage.getItem('mars_token')||'')}});localStorage.removeItem('mars_token');localStorage.removeItem('mars_selected_restaurant');showLogin()});app.querySelector('[data-action="account-back"]').addEventListener('click',connectPanel)}
function render(view='overview'){const map={overview:['Resumen',overview],restaurants:['Restaurantes',restaurantsView],subscriptions:['Suscripciones',subscriptions],calls:['Llamadas AI',callsView],reports:['Reportes',reports]};const [t,fn]=map[view]||map.overview;title.textContent=t;app.innerHTML=fn();app.querySelectorAll('[data-action]').forEach(b=>b.addEventListener('click',async()=>{const a=b.dataset.action;if(a==='new')notify('Abriendo registro de restaurante');if(a==='plans')nav('subscriptions');if(a==='select')notify('Plan seleccionado; falta conectarlo a la cuenta');if(a==='billing')notify('Mercado Pago sigue pendiente; usa activación en efectivo');if(a==='detail')notify('Ficha del restaurante lista para abrir');if(a==='export')notify('Reporte preparado para exportar');if(a==='activate-cash'){const response=await fetch('/api/billing/activate-cash',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+(localStorage.getItem('mars_token')||'')},body:JSON.stringify({restaurant_id:b.dataset.id})});if(response.ok){notify('Pago en efectivo confirmado y cuenta activada');connectPanel();}else notify('No se pudo activar la cuenta')}if(a==='invite-owner'){const name=prompt('Nombre del dueño o responsable:');const email=prompt('Correo del dueño o responsable:');if(!name||!email)return;const response=await fetch('/api/invitations',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+(localStorage.getItem('mars_token')||'')},body:JSON.stringify({restaurant_id:b.dataset.id,name,email,role:'OWNER'})});const data=await response.json();if(response.ok)alert('Acceso creado. Contraseña temporal: '+data.temporary_password);else notify(data.error||'No se pudo crear el acceso')}}))}
render();
const loginScreen=document.querySelector('#login-screen');
function showLogin(){loginScreen.classList.remove('hidden')}
function hideLogin(){loginScreen.classList.add('hidden')}
async function login(event){event.preventDefault();const email=document.querySelector('#login-email').value;const password=document.querySelector('#login-password').value;try{const response=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const data=await response.json();if(!response.ok)throw new Error(data.error||'No se pudo iniciar sesión');localStorage.setItem('mars_token',data.token);hideLogin();notify('Sesión iniciada');connectPanel();}catch(e){notify(e.message)}}
document.querySelector('#login-form').addEventListener('submit',login);document.querySelector('#account-menu').addEventListener('click',accountView);
async function connectPanel(){try{const token=localStorage.getItem('mars_token')||'';const headers={'Authorization':'Bearer '+token};const meResponse=await fetch('/api/me',{headers});const me=meResponse.ok?(await meResponse.json()).user||{}:{};document.querySelector('#account-name').textContent=me.name||'Usuario';document.querySelector('#account-role').textContent=me.role==='ADMIN'?'Administrador':'Restaurante';const response=await fetch('/api/dashboard',{headers});if(response.status===401){showLogin();return}if(response.ok){const data=await response.json();const me=data.user||{};document.querySelector('#account-name').textContent=me.name||'Martín Arellano';document.querySelector('#account-role').textContent=me.role==='ADMIN'?'Administrador':'Restaurante';document.querySelectorAll('.nav').forEach(x=>x.style.display='');if(data.restaurants&&data.restaurants.length)restaurants=data.restaurants.map(r=>({id:r.id,name:r.name,city:r.city,plan:r.plan,status:r.status,users:r.users_count,minutes:`${r.minutes_used} / ${r.minutes_limit}`}));if(data.calls&&data.calls.length){const names=Object.fromEntries(data.restaurants.map(r=>[r.id,r.name]));calls=data.calls.map(c=>[names[c.restaurant_id]||'Restaurante','Llamada · '+c.duration_min+' min',c.result,c.confidence]);}render();notify('Panel administrativo conectado');return}const owner=await fetch('/api/my/dashboard',{headers});if(!owner.ok)throw new Error('offline');const ownerData=await owner.json();document.querySelector('#account-name').textContent=ownerData.user.name||'Usuario';document.querySelector('#account-role').textContent=ownerData.user.role==='ADMIN'?'Administrador':'Restaurante';document.querySelectorAll('.nav').forEach(x=>x.style.display='none');document.querySelector('.sidebar-foot').innerHTML='<span class="online"></span> Sistema operativo<br><small>Panel privado del restaurante</small>';document.querySelector('#page-title').textContent=ownerData.restaurants.find(x=>x.id===selectedRestaurantId)?.name||ownerData.restaurants[0].name;app.innerHTML=ownerDashboard(ownerData);const switcher=app.querySelector('#restaurant-switcher');if(switcher)switcher.addEventListener('change',()=>{selectedRestaurantId=switcher.value;localStorage.setItem('mars_selected_restaurant',selectedRestaurantId);connectPanel()});app.querySelectorAll('[data-action]').forEach(b=>b.addEventListener('click',()=>{if(b.dataset.action==='owner-menu')ownerEditor();else if(b.dataset.action==='owner-reservations'||b.dataset.action==='owner-calendar')ownerReservations();else if(b.dataset.action==='owner-feedback')ownerFeedback();else notify('La conexión individual de Calendar queda preparada para configurar')}));notify('Panel privado conectado');}catch(e){notify('Modo demostración: backend no conectado');}}
connectPanel();
